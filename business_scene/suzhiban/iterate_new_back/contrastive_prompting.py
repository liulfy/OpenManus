


analysis_prompt_template = """# Role
你是一位资深的业务流程分析师，擅长从非结构化投诉文本中提取关键业务规则。

# Task
我会为你提供两组投诉数据：
- 【集合 A】：真实标签为“下派”的样本。
- 【集合 B】：真实标签为“不下派”的样本。

请对比这两组数据，分析【集合 A】中投诉内容的共同特点，且这些特点在【集合 B】中通常不具备。

# Data
【集合 A - 下派样本】：
{assign_data}

【集合 B - 不下派样本】：
{no_assign_data}

# Output Format
请按以下维度输出分析结果：
1. **核心差异点**：一句话总结区分两者的“分水岭”是什么。
2. **下派样本共同特征清单**：
   - 特征项（如：诉求明确度、投诉升级信号、特定业务领域等）
   - 具体表现描述
   - 集合 A 中的证据示例
3. **排他性确认**：确认这些特征在集合 B 中为何不成立或表现不同。"""

induction_prompt = f"""# Role
你是一位逻辑架构师，负责将业务洞察转化为严谨的判定算法。

# Task
基于上一步提取的特征，请为“下派”标签制定一套闭环的判定逻辑。这套逻辑将用于自动化判断一条新的投诉内容是否需要“下派”。

# Context (上一步的分析结果)
[在此粘贴第一步输出的特征清单]

# Requirements
请输出一套结构化的判定准则，包含：
1. **强制下派准则 (Must-Have)**：只要满足其中任一条件，必须下派（例如：涉及监管机构、强烈的退费要求等）。
2. **排除准则 (Exclude)**：即便看似符合下派特征，但只要具备某些元素，则判定为“不下派”（例如：仅为咨询、无具体争议点等）。
3. **逻辑判定流程图 (Text-based)**：描述判断的先后顺序。

# Output Format
请使用“如果 [特征描述]，则 [判定结果]”的格式进行描述。"""

verify_prompt_template = """# Role
你是一位质量保证（QA）工程师，负责对判定规则进行压力测试和边界核查。

# Task
我现在给你一组【不下派】的投诉样本，请你使用给定的“判定准则”对它们进行模拟判定。

# 投诉样本
{complaint_data}

# 判定准则
{judge_rule}

# Instruction
1. 逐一检查这些样本是否会被你总结的准则误判为“下派”。
2. 如果发生误判，请分析是哪条准则定义过宽，并给出修正建议。
3. 最终输出一套优化后的、具备高区分度的“投诉下派判定终版规则”。

# Output Format
- 误判分析表（样本 ID -> 误判原因 -> 修正方案）
- 修正后的终版规则清单"""



# from model_api.doubao_seed_2_lite import query_doubao
from business_scene.suzhiban.back.info_extractor import wash_pending_content
import pandas as pd

df = pd.read_excel("12月清单抽取_省自定2_5000_10000.xlsx", engine="openpyxl")

data_size = len(df)
assign_list = []
no_assign_list = []
assign_index = 1
no_assign_index = 1
for index in range(data_size):
    row_data = df.iloc[index]
    identity_num = row_data['受理单号']
    content_1 = row_data['一级目录']
    content_2 = row_data['二级目录']
    pending_content = row_data['受理内容']
    to_assign = row_data['主单是否下派']
    if to_assign == "下派":
        assign_list.append(f"样本 {assign_index}：\n{pending_content}\n\n")
        assign_index += 1
    else:
        no_assign_list.append(f"样本 {no_assign_index}：\n{pending_content}\n\n")
        no_assign_index += 1

assign_data = ''.join(assign_list)
no_assign_data = ''.join(no_assign_list)
analysis_prompt = analysis_prompt_template.format(assign_data = assign_data, no_assign_data = no_assign_data)

generated_rule_1 = """1. 强制下派准则 (Must-Have)

只要满足以下任意一条，必须判定为「下派」：

如果投诉内容包含极端风险表述（包括但不限于扬言暴力伤害他人、自残自杀、危害公共安全、冲击营业场所等），则必须下派。
如果投诉内容明确提及已向工信部、通管局、12345 等监管部门提交投诉且已被受理，或明确要求监管部门介入处理，则必须下派。
如果诉求涉及普通前台服务人员无权限办理的特殊业务，包括但不限于：①政企 / 校园专属优惠的非标准化诉求（协议期内免违约金改档 / 续约、原档续约被拒后再次投诉、明确拒绝校园企微 / 营业厅等标准办理渠道）；②跨域业务（跨市移机、异名号码添加为副卡、异地特殊业务无法通过标准异地通道办理）；③特殊场景业务（公网 IP/IPv6 申请、销户超过 90 天要求复机、机主离世要求免公证过户、靓号协议异议、VIP 专属流量 / 折扣活动申请），则必须下派。
如果诉求涉及线下复杂争议需核实协调，包括但不限于：①线下营业厅办理时工作人员承诺内容与实际业务规则不符；②线下办理业务失败且营业厅明确告知无权限解决；③要求调取线下办理底单 / 录音 / 签字记录核实争议；④协议期内明确要求免违约金改套餐 / 销户 / 携号转网；⑤同一诉求已被受理处理 2 次及以上，用户仍重复投诉，则必须下派。
如果诉求涉及特殊群体或紧急场景无法适配标准流程，包括但不限于：①老年人、残障人士等特殊群体明确表示无法配合身份核验、线上操作等标准办理流程；②明确提出 24 小时内必须完成的紧急业务需求（如急救用呼叫转移、紧急复机、紧急携转 / 销户）且标准流程无法满足时效要求，则必须下派。

2. 排除准则 (Exclude)

未满足上述强制下派准则的前提下，只要满足以下任意一条，直接判定为「不下派」：

如果诉求仅为咨询类（包括询问业务规则、查询办理进度、无明确投诉或强制办理诉求），则判定为不下派。
如果诉求为可通过标准化流程直接受理的普通业务，包括但不限于：①误订增值业务（会员、点播费、通用权益包等）的退订退费；②普通套餐变更 / 销户 / 续约可通过线上官方渠道、标准线下流程办理；③首次提出校园业务诉求，可引导至校园企微 / 校园营业厅且用户未明确拒绝该渠道，则判定为不下派。
如果用户仅对电信公开统一的通用规则不满（如仅投诉通话记录仅可查询 6 个月、积分兑换门槛、通用业务规则本身，无具体个案争议），则判定为不下派。
如果用户仅口头提及「将投诉至监管部门 / 12345」但无实质受理凭证、无极端风险表述，且诉求属于可标准化受理范围，则判定为不下派。

3. 逻辑判定流程图 (Text-based)
步骤 1：强制下派优先校验

逐条匹配投诉内容与强制下派准则的所有条款：

如果匹配到任意一条：输出判定结果「下派」，流程结束；
如果未匹配到任何条款：进入步骤 2。
步骤 2：排除准则校验

逐条匹配投诉内容与排除准则的所有条款：

如果匹配到任意一条：输出判定结果「不下派」，流程结束；
如果未匹配到任何条款：进入步骤 3。
步骤 3：兜底判定

所有未触发强制下派、也未触发排除准则的投诉，默认输出判定结果「不下派」，流程结束。"""

FT_result_index = [1, 3, 9, 14, 19, 21, 22, 24, 29, 33, 44, 45, 47, 57, 59, 65, 72, 77, 82, 84, 87, 97]
df = pd.read_excel("12月清单抽取_省自定_5000_10000.xlsx", engine="openpyxl")
complaint_list = []
complaint_index = 1
for index in FT_result_index:
    this_index = index + 300
    row_data = df.iloc[this_index]
    identity_num = row_data['受理单号']
    content_1 = row_data['一级目录']
    content_2 = row_data['二级目录']
    pending_content = row_data['受理内容']
    # extracted_content = row_data['抽取内容']
    extracted_content = wash_pending_content(content_1, content_2, pending_content)
    complaint_list.append(f"样本 {complaint_index}：\n{extracted_content}\n\n")
    complaint_index += 1
    print(complaint_index)

complaint_data = ''.join(complaint_list)

verify_prompt = verify_prompt_template.format(complaint_data = complaint_data, judge_rule = generated_rule_1)

generated_rule_2 = """修正后的终版规则清单
1. 强制下派准则（Must-Have）

满足以下任意一条，必须判定为「下派」：
1.1 投诉内容包含极端风险表述，包括但不限于扬言暴力伤害他人、自残自杀、危害公共安全、冲击营业场所等。
1.2 投诉内容明确提及已向工信部、通管局、12345 等监管部门提交投诉且已被受理，或明确要求监管部门介入处理。
1.3 诉求涉及普通前台服务人员无权限办理的特殊业务，且无通用标准化流程可承接，属于以下任意一类：
1.3.1 政企 / 校园专属优惠的非标准化诉求：包括协议期内申请免违约金改档 / 续约、原档续约被拒后就同一诉求再次投诉、针对校园专属优惠 / 业务明确拒绝校园企微 / 校园营业厅等专属标准办理渠道且无通用渠道可承接；
1.3.2 跨域特殊业务：包括跨市移机、异名号码添加为副卡、异地特殊业务无法通过官方标准异地通道办理；
1.3.3 特殊场景诉求：包括公网 IP/IPv6 申请（无标准线上办理通道的）、因运营商过错导致销户超过 90 天用户要求复机、机主离世要求免公证过户、靓号协议异议、非公开的特定 VIP 群体专属流量 / 折扣活动申请（通用流程无法办理的）。
1.4 诉求涉及线下复杂争议需核实协调，属于以下任意一类：
1.4.1 线下营业厅办理时工作人员承诺内容与实际业务规则不符，用户提供相关凭证的；
1.4.2 线下办理业务失败且营业厅明确告知无权限解决的；
1.4.3 要求调取线下办理底单 / 录音 / 签字记录核实争议的；
1.4.4 协议期内明确要求免违约金改套餐 / 销户 / 携号转网，且无对应通用规则支持的；
1.4.5 同一诉求已有 2 次及以上完成全流程的正式处理记录并明确告知用户处理结果，用户仍重复投诉的。
1.5 诉求涉及特殊群体或紧急场景无法适配标准流程，属于以下任意一类：
1.5.1 老年人、残障人士等特殊群体明确表示无法配合身份核验、线上操作等标准办理流程，且无其他标准化便捷通道可承接的；
1.5.2 明确提出 24 小时内必须完成的紧急业务需求（如急救用呼叫转移、紧急复机、紧急携转 / 销户），且官方标准流程无法满足时效要求的。
2. 排除准则（Exclude）

未满足上述强制下派准则的前提下，满足以下任意一条，直接判定为「不下派」：
2.1 诉求仅为咨询类，包括询问业务规则、查询办理进度、无明确投诉或强制办理诉求的。
2.2 诉求为可通过标准化流程直接受理的普通业务，包括但不限于：
2.2.1 误订增值业务（会员、点播费、通用权益包等）的退订退费；
2.2.2 普通套餐变更 / 销户 / 续约 / 过户等业务，可通过线上官方渠道、标准线下流程办理的（含校园用户办理普通通用业务，可通过通用标准化渠道办理的场景）；
2.2.3 首次提出校园专属业务诉求，可引导至校园企微 / 校园营业厅等专属标准渠道且用户未明确拒绝该渠道的。
2.3 用户仅对电信公开统一的通用规则不满（如仅投诉通话记录仅可查询 6 个月、积分兑换门槛、通用业务规则本身，无具体个案争议或运营商过错举证）。
2.4 用户仅口头提及「将投诉至监管部门 / 12345」但无实质受理凭证、无极端风险表述，且诉求属于可标准化受理范围的。
3. 逻辑判定流程

步骤 1：强制下派优先校验：逐条匹配投诉内容与强制下派准则所有条款，匹配到任意一条则输出「下派」，流程结束；未匹配则进入步骤 2。
步骤 2：排除准则校验：逐条匹配投诉内容与排除准则所有条款，匹配到任意一条则输出「不下派」，流程结束；未匹配则进入步骤 3。
步骤 3：兜底判定：所有未触发上述两类规则的投诉，默认输出「不下派」，流程结束。"""


